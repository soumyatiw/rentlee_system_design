frontend/node_modules
backend/node_modules
frontend/.next
backend/dist

# Environment files — never commit real credentials
backend/.env
frontend/.env.local
.env
.env.local
.env.*.local
# .env.example and .env.local.example are safe to commit (no real values)